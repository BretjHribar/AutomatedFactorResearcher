{
    "_doc": "Binance 4h TOP100 factor-research universe. Same construction recipe as TOP30 (top-N by ADV, 60d rebalance, 365d min history, vol-rank-95 exclusion). Wider cross-section for more stable rank statistics on a deeper universe.",
    "exchange": "binance",
    "interval": "4h",
    "bars_per_day": 6,

    "universe": {
        "name": "BINANCE_TOP100_4h",
        "method": "top_n_by_adv",
        "top_n": 100,
        "rebalance_days": 60,
        "min_history_days": 365,

        "exclusion": {
            "method": "vol_rank",
            "field": "parkinson_volatility_60",
            "percentile_threshold": 95,
            "scope": "train_window"
        },

        "weighting": {
            "method": "equal"
        }
    },

    "data": {
        "matrices_dir": "data/binance_cache/matrices/4h",
        "adv_field": "adv20",
        "close_field": "close"
    },

    "splits": {
        "train_start": "2021-01-01",
        "train_end": "2025-09-01",
        "val_end": "2026-01-01"
    },

    "output": {
        "universe_dir": "data/binance_cache/universes",
        "filename_template": "{name}.parquet",
        "legacy_backup_suffix": ".legacy.parquet"
    }
}
