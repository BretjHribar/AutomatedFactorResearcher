name: Legacy CI

on:
  workflow_dispatch:

jobs:
  redirected:
    runs-on: ubuntu-latest
    steps:
      - name: Use Factor Alpha CI/CD
        run: |
          echo "The automated pipeline now lives in .github/workflows/factor-alpha-ci-cd.yml."
          echo "Run this legacy workflow only for historical/manual debugging."
